# 水下机器人巡游赛 —— 视觉系统实现路径

## 1. 文档目的

明确视觉系统**各任务阶段的实现手段与实现路径**：用哪个摄像头、用什么方法（HSV / YOLO）、输出什么、预期达到什么效果。作为后续开发、联调、验收的统一依据。

---

## 2. 硬件与运行环境

### 2.1 运行平台

| 项 | 说明 |
|---|---|
| 机载主控 | RDK S100（Ubuntu 22.04，ARM64，100TOPS BPU） |
| 摄像头 | 2 个 USB 摄像头（MJPEG，OpenCV/V4L2 采集） |
| 执行层 | STM32（串口 115200）+ 10 推进器混控 |
| 视觉→控制 | S100 上视觉输出检测结果 → 状态机 → 运动控制（PID）→ `$CMD` 帧 → STM32 |

### 2.2 摄像头分配（需实测确认）

| 摄像头 | 朝向 | 负责阶段 | 备注 |
|---|---|---|---|
| CAM1（前视） | 前向 | 撞球 / 穿门 /  触壁 | 物体识别 + 中心定位 |
| CAM2（下视） | 向下看池底 | 巡线 / 反向巡线 | 引导线偏移与角度 | 捡球 / 放球 | 物体识别 + 中心定位 |

> **待确认**：实际安装朝向、FOV、距池底高度。FOV 决定"物体在画面里多大"，直接影响距离估计标定。

### 2.3 方法选型原则（分层）

```
纯结构物（线/门/框）  ──► HSV + 形状/尺寸/位置/时间 多重验证（快、稳、输出精确几何）
外观多变物（球/环）   ──► YOLO 主 + HSV 兜底（抗光照、抗低对比、带分类）
```

理由：比赛目标物颜色固定，水下蓝绿背景对红/黄/橙对比度天然高；但光照不均、水浊、蓝球对蓝水、小球被背景吃掉等场景 HSV 会翻车，需要 YOLO 补位。

---

## 3. 系统架构

```
CAM1 前视 ─┐
           ├─► 视觉系统 (S100) ─► 检测结果 ─► 状态机(FSM) ─► 运动控制(PID) ─► $CMD ─► STM32
CAM2 下视 ─┘        │                                        ▲
                    │                                        │ 遥测(深度/IMU)
                    └────────────────────────────────────────┘
```

- 状态机原则：**视觉驱动**（看到什么做什么），任务切换须**连续 10~15 帧稳定**才生效。
- 强制顺序锁：未完成撞球前，屏蔽门/陈列架/收集框识别（规则强制先撞球）。

---

## 4. 各阶段实现明细

### P0 自主下潜（得分：10）

| 项 | 说明 |
|---|---|
| 摄像头 | 下视（确认池底/引导线出现） |
| 方法 | 深度控制走**压力传感器**（STM32 遥测 depth，非视觉）；垂向推进器定深 PID（运动控制侧） |
| 输出 | 下潜到位标志（深度≈距池底 60cm 中层） |
| 预期效果 | 全程无部件出水；下潜后下视能检出引导线始端 |

---

### P1 巡线（导航基础，已实现）

| 项 | 说明 |
|---|---|
| 摄像头 | 下视 |
| 方法 | HSV 橙红分割 → 形态学（开+闭）→ 最大轮廓 → 图像矩/`fitLine` 求 offset、angle |
| 增强项 | ① 自适应阈值（Otsu 对 S/V 通道），替代写死 HSV；② 现场 10 分钟调试窗口重新标定 |
| 输出 | `offset`（线重心偏离画面中心，px）、`angle`（线偏角，°）、`detected` |
| 预期效果 | 正常光况检出率 ≥ 95%；offset 误差 ≤ ±20px；单帧耗时 ≤ 20ms（≥ 15FPS） |
| 风险 | 池底反光/水浊断线 → 闭运算弥合 + 丢失帧降级策略（沿用 `last_valid`） |

---

### P2 撞球（得分：30/错误 10，**强制前置任务**）

| 项 | 说明 |
|---|---|
| 摄像头 | 前视 |
| 主检测 | **YOLO**（类别 `red_ball` / `blue_ball`，抽签颜色二选一） |
| 兜底 | HSV（红/蓝圆 + 圆度≥0.85）——YOLO 连续 N 帧无检测时启用 |
| 撞球成功判定 | YOLO 确认"球在撞击前位置 → 撞击后离开原位置/位移" + 推进器满推力电流记录（双重验证，避免误判） |
| 输出 | 球心归一化偏移、距离估计（由 bbox 尺寸反算，球半径 10cm 已知）、conf |
| 预期效果 | 1.2m 内球检出率 ≥ 95%；球心定位误差 ≤ ±30px；单帧 ≤ 80ms（≥ 10FPS） |
| 风险 | 蓝色球对蓝水低对比 → **这是 YOLO 主路线的核心价值**；域差需水下实测 |

---

### P3 穿门（得分：30/个，接触 10/个）

| 项 | 说明 |
|---|---|
| 摄像头 | 前视 |
| 方法 | HSV 红 → 形状判别 **"Π 形"**（两竖杆 + 顶部横梁，内部空心）→ 判门类型（高门/矮门）→ 输出门洞中心 |
| 输出 | 门中心归一化偏移、门类型（high/low）、是否对齐 |
| 预期效果 | 门识别率 ≥ 90%；门中心偏差 ≤ ±30px |
| 风险 | 门/收集框/圆环同为红色 → **靠形状判别表区分**（见 §6）；穿门时接触得 10 分也比卡住强，控制端以稳为先 |

---

### P4 捡球（得分：正确 100/个，错误 50/个，**2026 新增圆环**）

| 项 | 说明 |
|---|---|
| 摄像头 | 前视（对准陈列架） |
| 主检测 | **YOLO**（类别 `yellow_golf` / `pink_golf` / `red_ring`） |
| 兜底 | HSV：黄/粉实心小圆（高尔夫球）；红空心圆（圆环，外轮廓圆 + 中心空洞） |
| 抓取输出 | 目标中心 + 距离，换算成机械臂抓取点坐标（手眼标定后） |
| 预期效果 | 高尔夫球检出率 ≥ 90%；圆环检出率 ≥ 85%（水下实测后校正）；抓取点定位 ≤ ±20px |
| 风险 | ① 圆环为 2026 新增，样本少、微负浮力会漂 → 需采集真实水池素材；② 正确/错误颜色由**抽签**决定，识别必须给出颜色分类而非只出"球" |

---

### P5 放球（得分：100/个）

| 项 | 说明 |
|---|---|
| 摄像头 | 前视 |
| 方法 | HSV 红 → **闭合矩形边框**识别（30×40cm，内部空洞）→ 输出框口中心 + 朝向 |
| 输出 | 框口中心归一化偏移、是否到位 |
| 预期效果 | 收集框识别率 ≥ 90%；框口中心定位 ≤ ±30px |
| 风险 | 与门混淆 → 用"四边闭合矩形 + 尺寸"与"Π 形门"区分 |

---

### 返回出发区（得分：30）

| 项 | 说明 |
|---|---|
| 摄像头 | 下视（反向巡线）+ 前视/遥测（触壁确认） |
| 方法 | HSV 反向追踪引导线回到出发区角；触壁由运动控制侧（IMU 轴向撞击 / 碰撞判定）确认 |
| 输出 | 反向线 offset/angle；触壁标志 |
| 预期效果 | 反向巡线检出率 ≥ 90%；触壁确认可靠不误判 |

---

## 5. 统一检测输出接口

所有阶段输出统一结构，控制端（运动控制）只依赖此接口，不关心内部实现：

```python
@dataclass
class DetectionReport:
    task: str                     # "P1"~"P5" / "RETURN"
    obj_type: str                 # line / ball / door / box / golfball / ring
    color: str                    # red / blue / yellow / pink / none
    center_norm: tuple            # 相对画面中心归一化 [-1,1]，控制端可直接做偏差
    bbox: tuple                   # 像素框 (x1, y1, x2, y2)
    size_px: float                # 目标像素尺寸（用于距离反算）
    range_m: float                # 估计距离（已知实际尺寸反算，随阶段标定）
    conf: float                   # 置信度
    extra: dict                   # 任务特有：offset/angle / door_type / ring_center / …
```

---

## 6. 红色物体判别表（防混淆核心设计）

| 物体 | 摄像头 | 颜色 | 形状判据 | 尺寸先验 |
|---|---|---|---|---|
| 引导线 | 下视 | 橙红 | 细长条带，长宽比 > 5 | 宽 10cm |
| 撞球 | 前视 | 红/蓝 | 实心圆，圆度 ≥ 0.85 | 半径 10cm |
| 门 | 前视 | 红 | "Π 形"：两竖杆+横梁，内部空心 | 70×50cm |
| 收集框 | 前视 | 红 | 闭合矩形边框（四边），内部空洞 | 30×40cm |
| 圆环 | 前视 | 红 | 空心圆：外轮廓圆 + 中心洞 | 外径 12cm |

判别顺序：**先颜色**（是否红/黄/粉/蓝）→ **再形状**（实心/空心/长条/Π）→ **再尺寸** → **再看出现摄像头**。

---

## 7. YOLO 模型方案（**现模型不可直接用**）

### 7.1 现状排查结果

- 现有 `YOLO/ball-try/best.pt` 训练类别：`tennis-ball` / `not_tennis-ball`
- 数据来源：Roboflow 公开**网球数据集**（陆地图像，6209 张），训练 `imgsz=320`
- **结论：无法识别比赛目标物（撞球/高尔夫球/圆环），仅验证了 YOLO 流程可跑通。比赛前必须重训。**

### 7.2 需要的模型与类别

| 模型 | 类别 | 用于 |
|---|---|---|
| 撞球模型 | `red_ball` / `blue_ball` | P2 撞球（抽签决定目标颜色） |
| 目标物模型 | `yellow_golf` / `pink_golf` / `red_ring` | P4 捡球（区分正确/错误颜色） |

### 7.3 数据与训练计划

1. **真实水下素材优先**：用 S100 + 摄像头在水池拍摄球/环/门/框视频，抽帧标注。
2. 若水下样本不足：先用地表素材粗训（黄/粉球、红色圆环在地表也能拍到），**水下实测后再微调**。
3. 标注类别严格按 7.2；撞球模型与目标物模型分开训练。
4. 评估指标：mAP@0.5 ≥ 0.9 再上真机；预留 20% 水下真值做测试集。

### 7.4 部署路径（先通后优）

```
第一步：ONNX Runtime 在 S100 CPU 跑通（能出结果即可，10FPS 够用）
第二步：HobotModelConvert 转 BPU .bin，用 BPU 加速（省出 CPU 给巡线/控制）
```

> 水下机器人运动慢，10~15FPS 已满足控制需求；BPU 转换是后期优化，不要一开始卡在这里。

---

## 8. 关键技术对策汇总

| 问题 | 对策 |
|---|---|
| 光照不均 / 水浊 | 自适应阈值（Otsu）+ 现场 10 分钟标定 + 时间一致性滤波 |
| 红色物体互相混淆 | §6 颜色→形状→尺寸→摄像头 判别链 |
| HSV 漏检 | YOLO 兜底 + 降级策略（连续 N 帧无检测 → 搜索模式：原地旋转扫描） |
| YOLO 漏检/误检 | HSV 兜底 + 时间滤波（连续 10~15 帧）+ 置信度阈值 |
| 撞球成功误判 | YOLO 球位置位移 + 推进器电流 双重验证 |
| 相机白平衡漂移 | 关闭自动白平衡，固定增益（测试确定） |

---

## 9. 开发里程碑（2026-08-13 ~ 10-16，约 9 周）

| 周次 | 内容 | 交付物 |
|---|---|---|
| W1 | S100 环境搭建（Python/OpenCV）、摄像头可用性验证、采集真实水池素材 | 环境可用 + 素材库 |
| W2 | P1 巡线 S100 实跑；撞球/目标物训练数据准备与标注 | P1 在 S100 上可运行 |
| W3~W4 | 撞球模型训练评估；P2 视觉+运动控制联调 | P2 能撞到球 |
| W5 | P3 穿门视觉 + 联调；P4 高尔夫球检测 | P3 可穿门、P4 可定位球 |
| W6 | P4 圆环检测与抓取；P5 放球视觉 | P4 圆环、P5 可定位框 |
| W7 | 返回出发区 + 全流程状态机串联 | 完整流程一次跑通 |
| W8~W9 | 现场标定流程、模拟赛、3 次比赛机会战术演练 | 赛前冻结版本 |

---

## 10. 待确认问题

1. 摄像头实际安装朝向 / FOV / 距池底高度（决定距离估计标定）。
2. S100 是否已装 Python + OpenCV，USB 摄像头 `cv2.VideoCapture` 直接可用。
3. 撞球抽签颜色是否红蓝都可能抽到 → 蓝色球识别优先级。
4. 圆环是否有实物/照片素材供采集标注。
5. 手眼标定方式（机械臂抓取点与视觉坐标的换算）。
