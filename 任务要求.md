# 视觉系统任务要求

> 依据 `D:\RC\2026-11.1水下机器人巡游规则.docx`，拆分出需要视觉实现的任务。
> 本文档对原始要求进行整理：系统组成 → 任务模块 → 阶段流程 → 切换条件 → 代码进度 → 待确认问题。

---

## 1. 系统组成

| 组件 | 说明 |
|------|------|
| 主控板 | RDK S100 |
| 前置摄像头 | USB 摄像头，索引 **idx=0**，负责前方任务（撞球、穿门） |
| 下置摄像头 | USB 摄像头，索引 **idx=1**，负责底部任务（巡线、抓球、放球） |

- 开机后视觉系统开始工作，两个摄像头自动初始化，任一缺失不影响整体运行。

---

## 2. 任务模块总览

| 编号 | 任务 | 核心功能 | 使用摄像头 | 代码状态 |
|------|------|----------|:----------:|:--------:|
| P0 | 预处理 | 通用水下图像预处理（缩放 / 颜色校正 / 去噪 / CLAHE），供各任务共享调用 | 共享 | ✅ 已完成 |
| P1 | 巡线 | 检测池底橙红色引导线，输出偏移量 / 角度 | 下置 (idx=1) | ✅ 已完成 |
| P2 | 撞球 | YOLO 检测目标球，输出转向 / 距离 / 撞击决策 | 前置 (idx=0) | ✅ 已完成 |
| P3 | 穿门 | HSV + Π形验证检测高低门，输出门洞中心 | 前置 (idx=0) | ✅ 已完成 |
| P4 | 抓球 | 捡球 / 抓取目标球 | 下置 (idx=1) | ⏳ 未完成，计划复用 P2 代码 |
| P5 | 放球 | HSV + 闭合矩形验证识别红色篮筐，输出框口中心 | 下置 (idx=1) | ✅ 已完成 |
| P6 | 返航 | 利用 IMU 返回起点 | 不依赖摄像头 | ⏳ 未完成 |

> 注：P0 为共享预处理模块，后续任务（P1-P5）执行时内部会自动先调用 P0。

---

## 3. 比赛阶段流程

比赛共 **7 个阶段**，按顺序推进。每个阶段由"前置任务 + 下置任务"组合而成：

| 阶段 | 名称 | 前置 (idx=0) | 下置 (idx=1) | 进入下一阶段的条件 |
|:----:|------|:------------:|:------------:|--------------------|
| 一 | 巡线到达撞球区 | P2 撞球 | P1 巡线 | 前置连续 20 帧稳定识别到小球 |
| 二 | 撞球 | P2 撞球 | P1 暂停 | 撞球完成 |
| 三 | 巡线穿门至捡球区 | P3 穿门 | P1 巡线 | 前置连续 20 帧未识别到门 |
| 四 | 捡球 | P3 穿门（识别到球后暂停） | P1 巡线 + P4 抓球 并行 | 捡球完成 |
| 五 | 巡线至放球区 | — | P5 定位放球区 | 定位到放球区 |
| 六 | 放球 | — | P5 放球 | 放球完成 |
| 七 | 返航 | — | IMU 导航 | 到达起点 |

### 阶段流程图

```
开机 → 摄像头初始化
  │
  ▼
【阶段一】前置:P2 撞球 ｜ 下置:P1 巡线
  │   ← 前置连续 20 帧稳定识别到小球
  ▼
【阶段二】撞球（下置 P1 暂停）
  │   ← 撞球完成
  ▼
【阶段三】前置:P3 穿门 ｜ 下置:P1 巡线
  │   ← 前置连续 20 帧未识别到门
  ▼
【阶段四】下置:P1 巡线 + P4 抓球 ｜ 前置:P3
  │   ← 捡球完成
  ▼
【阶段五】下置:P5 定位放球区
  │   ← 定位到放球区
  ▼
【阶段六】下置:P5 放球
  │   ← 放球完成
  ▼
【阶段七】IMU 返航
```

> 注：阶段三 → 四 原文为"前视若20帧为识别到门"，按上下文理解为**连续 20 帧未识别到门**（表示已穿过门，不再看到门框）。

### 阶段切换的 20 帧判定规则

| 位置 | 判定条件 | 触发动作 |
|------|----------|----------|
| 阶段一 → 二 | 前置连续 **20 帧稳定识别**到小球 | 下置暂停 P1，进入撞球 |
| 阶段三 → 四 | 前置连续 **20 帧未识别**到门 | 进入捡球阶段 |
| 阶段四内部 | P4 连续 **20 帧稳定识别**到目标 | 前置暂停 P3 |

---

## 4. 当前代码进度

- 已完成：P0、P1、P2、P3、P5
- 未完成：P4（计划复用 P2 的检测代码）、P6（返航）
- 集成入口：`visual_main.py`（各任务模块独立编程后接入）

---

## 5. 待确认 / 待实现问题

1. **阶段五导航方式**：如何从捡球区移动到放球区？原文标注"好像不知道怎么到放球区"——是靠 P1 巡线引导、直接靠 P5 定位篮筐、还是其他方式？
2. **P4 抓球**：具体抓取判定逻辑，以及如何复用 P2 代码？
3. **P6 返航**：IMU 返航的详细流程，视觉是否需要参与？
4. **撞球动作执行方**：P2 输出转向 / 撞击决策后，实际撞击动作由机器人主控执行，还是视觉直接触发？
